#set ($page_title="Suite Overview")
#parse("pageHead.vm")

#if($treeRoot)
	#set ($branch=$treeRoot)
	#if ($branch.branches.size()==1)
	  #set ($branch=$branch.getBranches().get(0))
	  <div>
	  <span class="meta $branch.getCssClass()">$branch.getName() ($branch.getTestsPassed(), $branch.getTestsUnrun(), $branch.getTestsFailed()) </span>
	  <div class="collapsable">
	  	#parse("branchCompletion.vm")
	  </div>
	  </div>
	#else
		#parse("branchCompletion.vm")
	#end
#else
  <h2>Sorry no information: $pageTitle.Title</h2>
#end

#parse("pageFooter.vm")
